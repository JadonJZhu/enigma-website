// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    // Your GitHub username and repository name
    site: 'https://JadonJZhu.github.io',
    base: '/enigma-website',
  });
